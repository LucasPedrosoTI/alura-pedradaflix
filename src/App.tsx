import React from "react";
import Routes from "./routes";

const App = () => {
  return (
    <div style={styles}>
      <Routes />
    </div>
  );
};

const styles = {
  background: "#141414",
  flex: 1,
};

export default App;
